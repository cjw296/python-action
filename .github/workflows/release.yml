name: "Release"

on:
  workflow_run:
    workflows: [Continuous Integration]
    types: [completed]
    branches: [main]

jobs:
  release:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    permissions:
      contents: write
    steps:
      - uses: actions/setup-python@v4
        with:
          python-version: 3.12

      - uses: actions/checkout@v4

      - name: Install carthorse
        run: pip install carthorse
        shell: bash

      - name: Run carthorse
        run: carthorse --config .carthorse.yaml
        shell: bash

      - name: Run cjw296/python-workflow CI
        uses: peter-evans/repository-dispatch@v4
        with:
          token: ${{ secrets.PYTHON_WORKFLOW_CI }}
          repository: cjw296/python-workflow
          event-type: python-action-release
